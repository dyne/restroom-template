rule check version 1.0.0
rule unknown ignore

Scenario 'Restroom-mw': Save some random value inside a database (sqlite3, postgres, mysql, maridb, mssql) 

Given I have a database connection named 'myUri'
Given I have a database table named 'myTable'
When I create the array of '5' random objects of '256' bits
Then print the 'array'
Then save the result into the database 'myUri' into the table 'myTable'

